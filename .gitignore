# Terraform state files (store infrastructure state, often contain sensitive data)
*.tfstate
*.tfstate.backup

# Terraform plan files (output of terraform plan)
*.tfplan

# Terraform provider cache (generated by Terraform)
.terraform/

# Override files containing personal credentials
terraform.tfvars
terraform.tfvars.json

# Terraform logs and temporary files
crash.log
