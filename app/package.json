{
  "name": "fintech-app",
  "version": "1.0.0",
  "description": "Real-time financial analytics and payment API",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "lint": "eslint . --fix",
    "test": "NODE_ENV=test mocha 'test/**/*.js' --timeout 5000",
    "test:watch": "NODE_ENV=test mocha 'test/**/*.js' --watch",
    "test:ci": "NODE_ENV=test mocha 'test/**/*.js' --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results.xml"
  },
  "dependencies": {
    "chai": "^4.3.8",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "joi": "^17.13.3",
    "mocha": "^11.5.0",
    "morgan": "^1.10.0",
    "prom-client": "^15.1.3",
    "supertest": "^6.3.4"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "eslint": "^9.27.0",
    "mocha": "^11.5.0",
    "mocha-junit-reporter": "^2.2.1",
    "supertest": "^6.3.4"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
